export enum AuthenticationState {
    LOGGED_IN = 'logged in',
    LOGGED_OUT = 'logged out'
}

export enum Role {
    TEST_ROLE = 'Test Role'
}

export enum Page {
    LOGIN = '#/login',
}

export enum ItemActivity {
    CREATE = 'create',
    EDIT = 'edit',
    DELETE = 'delete'
}